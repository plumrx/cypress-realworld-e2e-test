
describe('setting', () => {

    context.skip('logout', () => {

        it("layout-1", () => {
            // cy.contains('settings').click()
            // cy.contains('Or click here to logout.').click()
        })
    })
})